const Weather  = (props) => {
    let coordinates = props.coordinates
    console.log(coordinates);
    
    return (

    <>
    <h1>Hello from the Weather component</h1>
    </> 
    );
}
 
export default Weather;